{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "starter.serviceAccountName" . }}
  labels:
{{ include "starter.labels" . | nindent 4 }}
{{- end -}}
